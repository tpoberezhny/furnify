export interface carusel {
  title: string;
  titleImage: any;
}

export interface TypeWriter {
  strings: string[];
  autoStart: boolean;
  loop: boolean;
  deleteSpeed: number;
  delay: number;
  cursor: string;
}
